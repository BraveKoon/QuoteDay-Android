<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 안드로이드 13부터 알림은 사용자가 따로 허락해야 한다. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- 기기를 껐다 켜면 걸어 둔 알람이 모두 사라진다. 다시 걸기 위해 필요하다. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
        정확한 시각의 알람. 안드로이드 14부터는 자동으로 주어지지 않으므로,
        허락되지 않으면 창(window) 알람으로 떨어진다(QuoteNotifications 참고).
    -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".QuoteDayApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/Theme.QuoteDay">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.QuoteDay">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 위젯과 알림이 여는 딥링크. quoteday://quote/<id> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="quoteday" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".notifications.QuoteAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".widget.QuoteWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_label">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/quote_widget_info" />
        </receiver>

        <receiver
            android:name=".notifications.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
